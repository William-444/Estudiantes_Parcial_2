CREATE DATABASE Parcial2
GO
USE Parcial2
GO

CREATE TABLE Alumno (
Id INT IDENTITY(1,1) PRIMARY KEY,
Nombre VARCHAR(100) NOT NULL,
Codigo VARCHAR(10) NOT NULL,
Laboratorio DECIMAL(4,2) NOT NULL,
Parcial DECIMAL(4,2) NOT NULL,
Asistencia DECIMAL(4,2) NOT NULL,
NotaFinal DECIMAL(4,2) NOT NULL,
Activo BIT NOT NULL CONSTRAINT DF_Alumno_Activo DEFAULT (1)
);

SELECT * FROM Alumno